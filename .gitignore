# Assistant config is SHARED with the team (CLAUDE.md, .claude/agents/,
# .claude/settings.json) — only per-developer local state stays untracked.
.claude/agent-memory/
.claude/settings.local.json

# Local editor / LSP config — kept on disk, untracked (root origin is the personal
# docs repo; per-IDE LSP wiring is developer-local). See LSP setup notes.
.obsidian/
.vscode/
.helix/
.editor/
.serena/

# Nested independently-versioned repos — each has its own .git and its own
# RST-Holdings remote; the meta-repo must NOT track or stage them.
/common-actions/
/docs/
/infrastructure-live/
/routeplane/
/routeplane-skills/
/terraform-modules/
